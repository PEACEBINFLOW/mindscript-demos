# 5.1-openai-flow.md  
## Demo: MindScript + OpenAI

High level:

- Configure OpenAI adapter in `mindscript-runtime`.
- Use an `.ms.md` file for a staged reasoning task.
- Log outputs into ledger.
- Optionally, search via `mindscript-search`.

This file documents:
- sample config
- sample CLI commands
- expected behaviors
